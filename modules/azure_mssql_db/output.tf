// This module does not create MSSQL servers. The `mssql_server_ids` map is
// provided into this module via the `mssql_server_ids` input variable by the
// caller (Environment). Removed incorrect output that referenced resources
// from another module.

// No outputs currently exported from this module.
